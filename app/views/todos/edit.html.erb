<h1>Edit Your Todo</h1>
<%= form_for @todo, html: {onsubmit: "return validateForm(this);", class: "todo_form"} do |f| %>
  <%= render "form", f: f%>
  <%= f.submit "Update", class: "btn btn-primary" %>
<% end %>
<br>
<%= link_to "Back To Todo List",todos_path, class: "btn btn-info"%>
